# =========================================
# 앱 환경 설정 파일
# 위치: /home/jihun/conf/app.conf
# Start.sh가 실행 시 이 파일을 읽어 환경변수로 주입한다.
# 비밀번호가 들어가므로 권한을 제한할 것: chmod 600 conf/app.conf
# =========================================

# 서비스 포트 (EC2 보안그룹에 열려있는 포트)
SERVER_PORT=8080

# === AWS RDS(MySQL) 연동 시 아래 주석(#) 해제 후 실제 값 입력 ===
# 주석 상태로 실행하면 기본 프로파일(H2 인메모리)로 동작한다.
#SPRING_PROFILES_ACTIVE=prod
#DB_HOST=여기에-RDS-엔드포인트-입력
#DB_NAME=messagedb
#DB_USER=admin
#DB_PASSWORD=여기에-비밀번호-입력
